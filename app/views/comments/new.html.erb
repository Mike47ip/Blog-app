<h2>Add a Comment</h2>

<%= form_with model: @comment, url: "/user/id/post/#{params[:post_id]}/comments/create" do |form| %>
  <%= form.label :text, 'Your comment' %>
  <%= form.text_field :text %>
  <br/>
  <%= form.submit %>
<%end%>
